# Environment
.env

# Firebase — service account files (generated per-environment, never commit)
google-services.json
GoogleService-Info.plist
ios/Runner/GoogleService-Info.plist

# Flutter/Dart — generated, installable via `flutter pub get`
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart
*.freezed.dart

# Android — generated/local
android/.gradle/
android/local.properties
android/key.properties
*.keystore
*.jks

# iOS — generated/installable via `pod install`
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec

# Node — installable via `npm install`
node_modules/
appium_tests/node_modules/

# IDE
.idea/
.vscode/
*.iml

# Unused Flutter platforms (mobile-only project)
linux/
macos/
web/
windows/

# macOS
.DS_Store

# Generated by `flutter pub get` — reproducible via pubspec.yaml
pubspec.lock

# Logs & crash dumps
*.log
